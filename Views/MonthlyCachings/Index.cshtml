@model IEnumerable<WebApplication1.Models.MonthlyCaching>

@{
    ViewBag.Title = "Index";
}

@*<h2>الصرف الشهري</h2>*@

<p>
    @if (Request.IsAuthenticated)
    {
        if (User.IsInRole("Administrator"))
        {
            @Html.ActionLink("إضافة صرف شهري جديد", "Create", null,htmlAttributes: new { @class = "btn btn-primary" })

        }
        if (User.IsInRole("المسئولون"))
        {
            @Html.ActionLink("إضافة صرف شهري جديد", "Create", null,htmlAttributes: new { @class = "btn btn-primary" })
        }

    }

</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Patient.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DateCreated)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Cost)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.status)
        </th>
        @if (Request.IsAuthenticated)
        {
            if (User.IsInRole("Administrator"))
            {
                <th>

                </th>

            }
            if (User.IsInRole("المسئولون"))
            {
                <th>

                </th>
            }

        }
    </tr>

@foreach (var item in Model) {
<tr>
    <td>
        @Html.DisplayFor(modelItem => item.Patient.Name)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.DateCreated)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.Cost)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.status)
    </td>
     @if (Request.IsAuthenticated)
    {
        if (User.IsInRole("Administrator"))
        {
<td>
    @Html.ActionLink("تعديل", "Edit", new { id = item.PatientID }, htmlAttributes: new { @class = "btn btn-primary" }) |
    @Html.ActionLink("تفاصيل", "Details", new { id = item.PatientID }, htmlAttributes: new { @class = "btn btn-success" }) |
    @Html.ActionLink("حذف", "Delete", new { id = item.PatientID }, htmlAttributes: new { @class = "btn btn-danger" })

  
</td>

        }
        if (User.IsInRole("المسئولون"))
        {
<td>

    @Html.ActionLink("تعديل", "Edit", new { id = item.PatientID }, htmlAttributes: new { @class = "btn btn-primary" }) |
    @Html.ActionLink("تفاصيل", "Details", new { id = item.PatientID }, htmlAttributes: new { @class = "btn btn-success" }) |
    @Html.ActionLink("حذف", "Delete", new { id = item.PatientID }, htmlAttributes: new { @class = "btn btn-danger" })

</td>
        }

    }
</tr>
}

</table>
